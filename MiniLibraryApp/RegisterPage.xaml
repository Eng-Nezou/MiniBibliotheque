<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MiniLibraryApp.RegisterPage"
             Title="">

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Center">

            <!-- ===================== -->
            <!-- 🖼 Logo bibliothèque -->
            <!-- ===================== -->
            <Image Source="https://img.freepik.com/vecteurs-premium/logo-bibliotheque_1029707-2.jpg"
                   HeightRequest="120"
                   WidthRequest="120"
                   Aspect="AspectFit"
                   HorizontalOptions="Center"
                   Margin="0,0,0,20"/>

            <!-- ===================== -->
            <!-- 🔑 Formulaire inscription -->
            <!-- ===================== -->
            <Label Text="Créer un compte" 
                   FontSize="28" 
                   FontAttributes="Bold" 
                   HorizontalOptions="Center"/>

            <Entry x:Name="emailEntry" Placeholder="Email" Keyboard="Email"/>
            <Entry x:Name="passwordEntry" Placeholder="Mot de passe" IsPassword="True"/>
            <Entry x:Name="confirmEntry" Placeholder="Confirmer mot de passe" IsPassword="True"/>

            <Button Text="Créer un compte" 
                    BackgroundColor="#4CAF50"
                    TextColor="White"
                    Clicked="OnRegisterClicked"/>

            <!-- ===================== -->
            <!-- 🔗 Lien vers Login -->
            <!-- ===================== -->
            <HorizontalStackLayout Padding="0" HorizontalOptions="Center">
                <Label Text="Déjà un compte? " />
                <Label Text="Se connecter" TextColor="Blue">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnGoToLoginTapped"/>
                    </Label.GestureRecognizers>
                </Label>
            </HorizontalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
